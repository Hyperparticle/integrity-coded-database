# Integrity Coded Database

## Commands

There are several options available:

```
convert-data  [-f <path-to-file>] [-a <arg-list>] [--verify]
convert-query [-f <path-to-file>] [-a <arg-list>]
execute-query [-f <path-to-file>] [-a <arg-list>] [--convert] [--verify]
```

